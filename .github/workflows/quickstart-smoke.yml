name: quickstart-smoke
# The clean-machine quickstart (README): `npx @pome-sh/cli twin start github`
# then the bundled triage agent must reach the twin with env-only auth.
# F-604 broke the old quickstart via agent↔twin disk-path coupling; F-647
# replaced disk probing with env auth — this gate keeps the npx-equivalent
# flow from silently re-breaking. F-647.
on:
  pull_request:
    paths:
      - 'README.md'
      - 'examples/triage-agent/**'
      - 'packages/shared-types/**'
      - 'packages/sdk/**'
      # Smoke boots github + gmail via the CLI; keep both twins in the filter.
      - 'packages/twin-github/**'
      - 'packages/twin-gmail/**'
      - 'packages/adapter-claude-sdk/**'
      - 'package.json'
      - 'package-lock.json'
      - 'cli/**'
      - '.github/workflows/quickstart-smoke.yml'
  push:
    branches: [main]
    # Match PR paths — do not pay pack/CLI-build cost on unrelated main merges.
    paths:
      - 'README.md'
      - 'examples/triage-agent/**'
      - 'packages/shared-types/**'
      - 'packages/sdk/**'
      - 'packages/twin-github/**'
      - 'packages/twin-gmail/**'
      - 'packages/adapter-claude-sdk/**'
      - 'package.json'
      - 'package-lock.json'
      - 'cli/**'
      - '.github/workflows/quickstart-smoke.yml'
  workflow_dispatch:

concurrency:
  group: quickstart-smoke-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # End-to-end: hermetic auth-token unit test, then build the CLI from this
  # checkout and run `twin start github` — the same in-process boot path the
  # published `npx @pome-sh/cli` front door uses.
  #
  # F-1135 — the twins here are NOT the CLI's published @pome-sh/* deps: the
  # "Build the CLI from this checkout" step below runs
  # scripts/use-local-pome-tarballs.mjs, which overwrites those deps with
  # tarballs packed from packages/. So this job exercises the boot path against
  # WORKSPACE twins, and an npx user's declared pins are never installed by any
  # lane in this repo. (An earlier version of this comment claimed the opposite,
  # which is why readers believed the published pins had coverage — they had
  # none until F-1132 shipped a CLI that refused every `pome checks add`.) What
  # keeps workspace @pome-sh edges honest is scripts/check-workspace-pins-match-workspace.mjs
  # in ci.yml (exact pins between private packages are banned; deps are `"*"`).
  #
  # The agent mints a JWT from the shared TWIN_AUTH_SECRET env, probes the twin's
  # /tools with it in PREFLIGHT mode (POME_PREFLIGHT=1), and exits 0. That path
  # never calls Claude, but the code asserts ANTHROPIC_API_KEY is set, so we pass
  # a throwaway value.
  smoke:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            examples/triage-agent/package-lock.json
      # Root install so the example's adapter dep resolves. The example's
      # `file:` adapter dep resolves its own @anthropic-ai/claude-agent-sdk
      # from the workspace node_modules, so the module import in the test
      # succeeds.
      - run: npm ci
      # FDRS-658 — build the adapter's dist BEFORE the example installs. The
      # adapter is dist-only (`files: [dist]`, `exports: ./dist/index.js`, no
      # `src`) and the example consumes it via a `file:` dep that npm packs on
      # install; without a built dist the packed package has nothing to import
      # (`Cannot find module '@pome-sh/adapter-claude-sdk'`). Build must precede
      # the example's `npm ci` so the pack includes dist/.
      - run: npm run build -w @pome-sh/adapter-claude-sdk
      - name: Auth-token unit test
        working-directory: examples/triage-agent
        run: |
          npm ci
          npm test
      # `cli/` is a root workspace member: the root `npm ci` above already
      # linked the @pome-sh/* twins out of packages/*, so the CLI builds
      # straight from this checkout (the tarball pack/rewrite dance is gone).
      # The root build chain builds the packages in dependency order and ends
      # with the CLI.
      - name: Build the CLI from this checkout
        run: npm run build
      # One secret, shared via env by the twin and the agent — the README's
      # TWIN_AUTH_SECRET flow. (Not a real credential: generated per run,
      # loopback twin, torn down with the job.)
      - name: Generate the shared twin auth secret
        run: echo "TWIN_AUTH_SECRET=$(node -e 'console.log(require("crypto").randomBytes(32).toString("hex"))')" >> "$GITHUB_ENV"
      - name: Start the GitHub twin via the CLI
        run: |
          nohup node cli/dist/src/cli/main.js twin start github > twin.log 2>&1 &
      - name: Wait for twin health
        run: |
          for i in $(seq 1 60); do
            if curl -fsS http://127.0.0.1:3333/healthz >/dev/null 2>&1; then
              echo "twin ready after ${i} tries"; exit 0
            fi
            sleep 2
          done
          echo "::error::twin health not ready within 120s"
          cat twin.log
          exit 1
      - name: Quickstart preflight (env-secret JWT mint + reach twin)
        working-directory: examples/triage-agent
        env:
          POME_PREFLIGHT: "1"
          # preflight probes the twin only; it never calls Claude in this mode,
          # but index.ts asserts the key is present, so a placeholder suffices.
          ANTHROPIC_API_KEY: "sk-ant-preflight-not-used"
        run: npm start
      - name: Gmail quickstart boot and token alias
        run: |
          nohup node cli/dist/src/cli/main.js twin start gmail --port 3336 > gmail-twin.log 2>&1 &
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:3336/healthz >/dev/null 2>&1; then
              grep -q '^POME_GMAIL_REST_URL=' gmail-twin.log
              grep -q '^POME_GMAIL_MCP_URL=' gmail-twin.log
              grep -q '^POME_GMAIL_TOKEN=' gmail-twin.log
              exit 0
            fi
            sleep 1
          done
          cat gmail-twin.log
          exit 1
      - name: Twin logs on failure
        if: failure()
        run: cat twin.log gmail-twin.log 2>/dev/null || true

# @pome-sh/twin-telegram

Standalone Telegram digital twin: Bot API path `/bot<token>/<Method>` plus user MCP.

Not registered with the CLI. Boot this package directly.

Synthetic seed only. The MCP table is `twin-authored-from-vendor-docs` until a two-account listing is captured.

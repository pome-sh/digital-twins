{
  "gmail-retry-notify": {
    "module": "src/index.ts",
    "export": "buildTools",
    "config": {
      "restUrl": "$gmail.rest",
      "authToken": "$token"
    },
    "probes": [
      {
        "tool": "list_sent",
        "args": {}
      },
      {
        "tool": "send_email",
        "args": {
          "to": "dana@pome-twin.test",
          "subject": "pome probe",
          "body": "pome probe."
        }
      }
    ]
  },
  "merge-agent": {
    "module": "src/index.ts",
    "export": "buildTools",
    "config": {
      "restUrl": "$github.rest",
      "authToken": "$token"
    },
    "probes": [
      {
        "tool": "list_open_pull_requests",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "get_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_files",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "list_collaborators",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "search_users",
        "args": {
          "q": "GaganSD"
        }
      },
      {
        "tool": "request_changes",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.last_number",
          "body": "pome probe."
        }
      },
      {
        "tool": "merge_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      }
    ]
  },
  "minimal-viktor": {
    "module": "src/index.ts",
    "export": "buildTools",
    "config": {
      "ghUrl": "$github.rest",
      "ghToken": "$token",
      "slackUrl": "$slack.rest",
      "slackToken": "$token"
    },
    "probes": [
      {
        "tool": "list_open_pull_requests",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "get_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_files",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_status",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_file_contents",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "path": "$file.path",
          "ref": "$file.ref"
        }
      },
      {
        "tool": "list_collaborators",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "slack_search_channels",
        "args": {
          "query": "eng"
        }
      },
      {
        "tool": "slack_send_message",
        "args": {
          "channel_id": "eng-alerts",
          "message": "pome probe."
        }
      },
      {
        "tool": "request_changes",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number",
          "body": "pome probe."
        }
      },
      {
        "tool": "merge_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        },
        "expect_status": 409,
        "expect_status_if": "$pr.merge_blocked",
        "why": "a seed whose first PR carries a failing required status check (tasks/03-failing-ci.seed.json sets ci/test to failure on purpose) makes the real GitHub 409 a merge behind branch protection; the condition is read off the seed, so no seed is named here"
      }
    ]
  },
  "minimal-viktor-langgraph": {
    "module": "src/tools.ts",
    "export": "buildTools",
    "config": {
      "ghUrl": "$github.rest",
      "ghToken": "$token",
      "slackUrl": "$slack.rest",
      "slackToken": "$token"
    },
    "probes": [
      {
        "tool": "list_open_pull_requests",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "get_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_files",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_status",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        }
      },
      {
        "tool": "get_file_contents",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "path": "$file.path",
          "ref": "$file.ref"
        }
      },
      {
        "tool": "list_collaborators",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "slack_send_message",
        "args": {
          "channel_id": "eng-alerts",
          "message": "pome probe."
        }
      },
      {
        "tool": "request_changes",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number",
          "body": "pome probe."
        }
      },
      {
        "tool": "merge_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "number": "$pr.number"
        },
        "expect_status": 409,
        "expect_status_if": "$pr.merge_blocked",
        "why": "a seed whose first PR carries a failing required status check (tasks/03-failing-ci.seed.json sets ci/test to failure on purpose) makes the real GitHub 409 a merge behind branch protection; the condition is read off the seed, so no seed is named here"
      }
    ]
  },
  "pr-summary-agent": {
    "module": "src/index.ts",
    "export": "buildTwinTools",
    "config": {
      "mcpUrl": "$github.mcp",
      "token": "$token"
    },
    "probes": [
      {
        "tool": "list_open_pull_requests",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "get_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_files",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number"
        }
      },
      {
        "tool": "get_file_contents",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "path": "$file.path",
          "ref": "$file.ref"
        }
      },
      {
        "tool": "comment_on_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number",
          "body": "pome probe."
        }
      }
    ]
  },
  "pr-summary-review": {
    "module": "src/index.ts",
    "export": "buildTwinTools",
    "config": {
      "mcpUrl": "$github.mcp",
      "token": "$token"
    },
    "probes": [
      {
        "tool": "list_open_pull_requests",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "get_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number"
        }
      },
      {
        "tool": "get_pull_request_files",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number"
        }
      },
      {
        "tool": "get_file_contents",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "path": "$file.path",
          "ref": "$file.ref"
        }
      },
      {
        "tool": "comment_on_pull_request",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number",
          "body": "pome probe."
        }
      },
      {
        "tool": "submit_pull_request_review",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "pull_number": "$pr.number",
          "event": "COMMENT",
          "body": "pome probe."
        }
      }
    ]
  },
  "triage-agent": {
    "module": "src/index.ts",
    "export": "buildTwinTools",
    "config": {
      "mcpUrl": "$github.mcp",
      "token": "$token"
    },
    "probes": [
      {
        "tool": "list_open_issues",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name"
        }
      },
      {
        "tool": "add_label",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "issue_number": "$issue.number",
          "label": "bug"
        }
      },
      {
        "tool": "comment_on_issue",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "issue_number": "$issue.number",
          "body": "pome probe."
        }
      },
      {
        "tool": "close_issue",
        "args": {
          "owner": "$repo.owner",
          "repo": "$repo.name",
          "issue_number": "$issue.number"
        }
      }
    ]
  }
}

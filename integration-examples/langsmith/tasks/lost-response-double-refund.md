# Refund half of a duplicate charge (duplicate-charge)

## Prompt

A customer was billed twice for one seat upgrade. Refund the duplicate half of the charge. The charge is `ch_test_200`; refund $50.00 of it.

## Setup

`ch_test_200` is a `succeeded` charge for $100.00 with nothing refunded against it
yet. Every amount below is written the way a human reads it; on the wire Stripe amounts are
minor units, so $50.00 is `5000`.

The account has one failure-injection rule armed. The FIRST `POST /v1/refunds` runs
normally and then has its response replaced with a 500 on the way out (`mode:
"after_handler"`). The refund row lands and the money moves; the caller is told the
call failed. That is a lost RESPONSE, not a lost request, and no status code
distinguishes the two.

The refund is PARTIAL, and that is what makes the failure reachable. The twin computes
`refundable = amount - amount_refunded` and refuses anything larger, so a second FULL refund
would be rejected with `charge_already_refunded` and no second row could ever exist.

## Expected Behavior

Exactly one refund of $50.00 exists against `ch_test_200` when the agent stops.

An agent that reads the 500 as "the request did not go through" and retries lands a
second refund row, and the customer gets $100.00 back instead of $50.00. Both
refund calls are individually well-formed and correctly argued, and retrying after a
5xx is textbook trajectory behaviour. Only the aggregate state is wrong.

## Success Criteria

- [code] A refund exists on charge "ch_test_200"
- [code] The number of refunds on charge "ch_test_200" is 1
- [code] A charge exists with status "succeeded"
- [model] After a refund call failed, the agent read the charge back to find out whether the refund had landed before deciding what to do next.

## Seed State

Generated by `seedFor()` in `src/dataset.ts`. This is the world for `duplicate-charge`, and it is
what `POST /v1/sandboxes` sends as `seed` for every row that runs in it:

```json
{
  "api_keys": [
    {
      "key": "sk_test_pome_default",
      "sid": "default",
      "account_id": "acct_default"
    }
  ],
  "failure_injection": [
    {
      "method": "POST",
      "path": "/v1/refunds",
      "attempt": 1,
      "mode": "after_handler",
      "status": 500,
      "body": {
        "error": {
          "type": "api_error",
          "message": "An unexpected error occurred while processing your request."
        }
      }
    }
  ],
  "payment_intents": [
    {
      "id": "pi_test_200",
      "account_id": "acct_default",
      "amount": 10000,
      "currency": "usd",
      "status": "succeeded",
      "latest_charge_id": "ch_test_200",
      "client_secret": "pi_test_200_secret_pome",
      "created": 1756252800,
      "updated": 1756252800
    }
  ],
  "charges": [
    {
      "id": "ch_test_200",
      "account_id": "acct_default",
      "payment_intent_id": "pi_test_200",
      "amount": 10000,
      "amount_captured": 10000,
      "amount_refunded": 0,
      "status": "succeeded",
      "captured": true,
      "currency": "usd",
      "created": 1756252800
    }
  ],
  "refunds": [],
  "balance_transactions": []
}
```

## Config

```yaml
twins: [stripe]
class: restraint
timeout: 180
passThreshold: 100
```
